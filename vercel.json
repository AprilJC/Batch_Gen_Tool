{
  "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @batch-gen/client run build",
  "outputDirectory": "packages/client/dist",
  "functions": {
    "api/generate.ts": {
      "maxDuration": 60
    }
  }
}
